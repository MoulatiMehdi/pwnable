the code uses `arm` cpu architechture , which means that : 

`bl` : call function 
`rl` : return value 
`r0` to `r12` : registers 
`pc` : current address of the instruction + 8 (because in `arm` it saves up to 8 bytes)
